PORT=3000
VAULT_ROOT=/Users/shenzhiheng/Documents/obsidian_repo

# Required for remote/ngrok use. Generate a long random value.
# Example: openssl rand -hex 32
MCP_ACCESS_TOKEN=

# Optional comma-separated origins. Leave empty for local testing.
ALLOWED_ORIGINS=
